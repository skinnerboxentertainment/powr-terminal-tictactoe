# Archive

Pre-refactor analysis documents preserved for reference. These describe the
template's state before the structural changes in v0.1.0 (engine drift cleanup,
tool adapter split, shared core extraction, custom skills, /auto-build command).
